# Reference

Raw, original source artifacts — kept as-received, not synthesized. If a
document here conflicts with `docs/01-ERD.md` through `docs/06-specification.md`,
the numbered docs win; this folder is provenance, not the authoritative spec.

Nothing lives here yet. When a raw artifact shows up (an original DDL export,
a client-provided diagram source, a requirements doc), put it here instead of
the top-level `docs/` folder, and link to it from the numbered doc that
supersedes it.
