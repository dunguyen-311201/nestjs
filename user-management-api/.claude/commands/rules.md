---
description: Show the agent rules and quality gates for this project
---

Invoke the `project-rules` skill to load and display the full agent rules for this project.

Then confirm: "Rules loaded. Autonomous actions allowed: read files, edit in-scope files, run build/lint/test, branch and commit. Must ask before: adding deps, changing API contracts, deleting files, pushing, touching out-of-scope modules."
